server:
  port: 9001

spring:
  application:
    name: SurveyMicro

  datasource:
    url: jdbc:mysql://localhost:3306/pilot
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        hbm2ddl:
          auto: update
